type: runtime

use_mariadb: true

internal: "hard"

# 後々DBをセットアップする必要あり
startup: |
  yes | pacman -Sy rustup
  rustup install nightly
  rustup default nightly
  cargo --version

entrypoint: cargo run --release